server:
  port: 83

spring:
  application:
    name: nacos-order-consumer
  cloud:
    nacos:
      discovery:
        #        server-addr: localhost:8848
        server-addr: 192.168.214.129:80 #配置Nacos地址

service-url:
  nacos-user-service: http://nacos-payment-provider